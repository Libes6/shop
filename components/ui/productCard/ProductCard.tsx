import React, {FC, useEffect} from 'react';
import {ProductService} from "@/services/product/product.service";

interface IProductCardProps {
    slug:string
}
const ProductCard: FC<IProductCardProps> = ({slug}) => {

    return (
        <div>

        </div>
    );
};

export default ProductCard;